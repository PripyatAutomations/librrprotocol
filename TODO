- handle failed connections/nick in use (if using irc backend)/other common signon errors gracefully
    * Add a mechanism for prompting for and trying an alternate nick or at least generate one
  - Implement per-room user lists, so we can populate the tab completion
  - Use char *irc_to_tui_colors(const char *in);

[Merge]
* auth.c / newauth.c
* ws.c / ws.cli.c / wsnew.c
